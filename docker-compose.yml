

services:
  metoffice-weather:
    
    build:
      context: .
      dockerfile: ansible/docker/Dockerfile
    env_file:
      - .env
    command: > 
              sh -c "python manage.py makemigrations &&
              python manage.py migrate &&
              python manage.py runserver 0.0.0.0:8000"
    ports:
      - 8000:8000
    volumes:
      - ./:/usr/src/metoffice_weather
    restart: always

  metoffice-weather-worker:
    
    environment:
      - .env
    build:
      context: .
      dockerfile: ansible/docker/Dockerfile
    command: >
              celery -A metoffice_weather worker --loglevel=info
    volumes:
      - ./:/usr/src/metoffice_weather
      - static_volume:/usr/src/metoffice_weather/staticfiles
      - media_volume:/usr/src/metoffice_weather/media
    restart: always
  metoffice-weather-beat:
    
    build:
      context: .
      dockerfile: ansible/docker/Dockerfile
    environment:
      - .env
    command: >
              celery -A metoffice_weather beat --loglevel=info
    volumes:
      - ./:/usr/src/metoffice_weather
    restart: always
  redis:
    image: "redis:alpine"
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"

volumes:
  redis_data:
  static_volume:
  media_volume:

